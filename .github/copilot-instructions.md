# Rivals Poll Creator — GitHub Copilot

Read `AGENTS.md` and `README.md`. Push landed source to `main`. GitHub Releases only on version bumps; attach the Windows zip there. Never commit the exe to the tree.

The .NET Windows shell, OCR fallback and checked-in web bundle must remain consistent. Do not edit a hashed bundle blindly: trace the current patch/build source and packaged output. Preserve manual correction of uncertain costume names, whole-outfit crops, selectable cover backgrounds, proper portrait export dimensions and downloadable ZIP. No invented names or paid remote OCR.

Run the fixture-backed OCR harness only when matching screenshots exist; validate the actual exported cover, all portrait pages and ZIP, and use `build-release.ps1` on Windows for packaging. Say when those environments/fixtures were unavailable.
