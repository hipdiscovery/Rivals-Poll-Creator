# Rivals Poll Creator — GitHub Copilot

Troy switches ChatGPT/Grok. Pull `main` before edits; push `main` before you stop. Releases only on version bumps; attach the Windows zip. Never commit the exe.

Read `AGENTS.md` and `README.md`. The .NET Windows shell, OCR fallback and checked-in web bundle must remain consistent. Do not edit a hashed bundle blindly. No invented names or paid remote OCR.

Run the fixture-backed OCR harness only when matching screenshots exist; validate exported cover, portrait pages and ZIP; use `build-release.ps1` on Windows for packaging.
