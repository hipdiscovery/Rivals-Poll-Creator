---
applyTo: "src/RivalsPollCreator/*.cs,src/RivalsPollCreator/*.csproj,tests/RivalsPollCreator.OcrHarness/*.cs,tests/fixtures/manifest.json,build-release.ps1"
---

Read `AGENTS.md`, current source and relevant tests before editing. **native shell and ocr:** Treat screenshot names as OCR evidence, not truth. Check costume owner versus title, Battle Pass/Shop/Inspect/colorways, missing text and manual correction. Keep offline WinRT+Tesseract fallback. Verify .NET Windows shell startup, native file picker, ZIP export and actual matching local screenshot fixtures before claiming end-to-end success.
