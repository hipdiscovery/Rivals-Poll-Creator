---
paths:
  - "src/RivalsPollCreator/*.cs"
  - "src/RivalsPollCreator/*.csproj"
  - "tests/RivalsPollCreator.OcrHarness/*.cs"
  - "build-release.ps1"
---

Read `.github/instructions/native-shell-and-ocr.instructions.md` and relevant tests after inspecting current source. Root `AGENTS.md` and `CLAUDE.md` remain authoritative; do not duplicate the feature guidance here.
